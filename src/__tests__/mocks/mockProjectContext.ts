import { ProjectDetails, PipeSegment, EquipmentItem, FluidType, SupportConfig, BrandingConfig } from '@/lib/types';

export const mockProjectContextValue = {
    projectDetails: {
        projectName: 'Test Project',
        projectNumber: '2026-TEST',
        designer: 'Test Designer',
        location: 'Test Location',
        beneficiary: 'Test Beneficiary',
        date: '2026-01-17',
        revision: '1',
    } as ProjectDetails,
    setProjectDetails: jest.fn(),
    segments: [] as PipeSegment[],
    setSegments: jest.fn(),
    addSegments: jest.fn(),
    equipmentList: [] as EquipmentItem[],
    setEquipmentList: jest.fn(),
    fluidType: 'water' as FluidType,
    setFluidType: jest.fn(),
    ifcModelUrl: null as string | null,
    setIfcModelUrl: jest.fn(),
    glycolPercentage: 0,
    setGlycolPercentage: jest.fn(),
    safetyMargin: true,
    setSafetyMargin: jest.fn(),
    safetyMarginPercentage: 5,
    setSafetyMarginPercentage: jest.fn(),
    fittingItems: [],
    setFittingItems: jest.fn(),
    activeTab: 'dashboard',
    setActiveTab: jest.fn(),
    supportConfig: {
        spacing: 2,
        mountingType: 'suspended',
        height: 1,
        pipesPerSupport: 1,
        insulationThickness: 0,
        insulationDensity: 0,
        addLeftConsole: false,
        addRightConsole: false,
        addUpperRail: false,
    } as SupportConfig,
    setSupportConfig: jest.fn(),
    branding: {
        primaryColor: '#000000',
        accentColor: '#ffffff',
        pdfTheme: 'modern',
    } as BrandingConfig,
    setBranding: jest.fn(),
    isInitialized: true,
    undo: jest.fn(),
    redo: jest.fn(),
    canUndo: false,
    canRedo: false,
    cloudProjectId: null,
    saveToCloud: jest.fn(),
    loadFromCloud: jest.fn(),
    foundPipes: [],
    setFoundPipes: jest.fn(),
    bimStatus: 'idle',
    setBimStatus: jest.fn(),
    parsingProgress: 0,
    setParsingProgress: jest.fn(),
};
